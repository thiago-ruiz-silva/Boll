using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//Script para passar os pontos (tempo) de uma cena para a outra
public class PassaPontos : MonoBehaviour {

    //Variável pontos para armazenar os pontos (Tempo)
    public static float pontos;

    //Variável (maximo) para armazenar o (Recorde)
    public static float maximo;

	public static bool isJogando;

	//Gambiarra para poder "zerar" os pontos que o (Jogador) fez, na tela de (Gameover)
	public static bool zerarPontos = false;

	// Use this for initialization
	void Start () {


		
	}
	
	// Update is called once per frame
	void Update () {
	
        

	}
}
